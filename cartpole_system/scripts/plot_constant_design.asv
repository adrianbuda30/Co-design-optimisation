% Create a box plot
boxplot(rewards, pole_length)
title('Constant ') % Adds a title to your figure
xlabel('Pole Length') % Adds a label to the x-axis
ylabel('Rewards') % Adds a label to the y-axis
hold off
